# Shadow Market assets

Drop finished Shadow Market art here using the exact filenames in `docs/SHOP-ART-BRIEF.md`.
The UI already points at these paths and keeps its CSS/glyph fallback when a file is absent.
Priority 1 is commands, celebrations and relic medallions. Do not rename shipped asset paths without a migration.